<h1>edit medication</h1>

<%= render 'form', med: @med %>

<div class="navbar">
  <%= link_to 'show', @med %>
  <%= link_to 'Back', request.referer.present? ? request.referer : default_path %>
</div>
